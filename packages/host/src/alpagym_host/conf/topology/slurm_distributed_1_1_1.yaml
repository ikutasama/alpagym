# @package _global_

defaults:
  - /execution/slurm/topology@execution.slurm.topology: separate_nodes
  # Cross-node disaggregated runs default to NCCL: rollout tensors flow over NCCL
  # between nodes. `override` replaces the `transport: disk` default in default.yaml.
  - override /transport: nccl
  - _self_

# Three 8-GPU nodes: one policy node, one rollout node, and one AlpaSim node.

execution:
  backend: slurm
  slurm:
    nodes: 3
    topology:
      cosmos_nodes: 2
      alpasim_nodes: 1

alpasim:
  wizard_args:
    topology: alpagym_8gpu

cosmos:
  mode: disaggregated
  launch:
    policy_replicas: 4
    rollout_replicas: 8
  train:
    train_batch_per_replica: 2
  policy:
    parallelism:
      dp_shard_size: 2
