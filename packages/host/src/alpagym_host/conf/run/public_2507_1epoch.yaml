# @package _global_

dataset:
  test_suite_id: public_2507
  scene_ids: null

alpasim:
  simulation_timeout_s: 1800.0

cosmos:
  # When using with 16 rollout replicas and 4 trainers:
  # batch_size * n_generation = 32
  # 32 / train_batch_per_replica = 8
  train:
    train_batch_per_replica: 8
    max_num_steps: null
    num_epochs: 1
    train_policy:
      mini_batch: 4
  rollout:
    n_generation: 2
    batch_size: 1
  logging:
    experiment_name: public_2507_1epoch
